import json

import yaml

from samsara import banners, fandom


def main() -> None:
    # fandom.MaxContinues = 5
    # with open("mystuff.yaml", "w") as f:
    #     f.write(yaml.dump(fandom.get_event_wishes(), default_flow_style=False))
    # with open("mystuff.json", "w") as f:
    #     f.write(json.dumps(fandom.get_5_star_weapons(), indent=2))
    pass


if __name__ == "__main__":
    main()
